{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://maxai.com/schemas/credentials/ghl-agency-api-key.schema.json",
  "title": "GHL Agency API Key Credential",
  "description": "Agency-level API key credential set for GHL authentication (future adapter)",
  "type": "object",
  "properties": {
    "apiKey": {
      "type": "string",
      "minLength": 20,
      "description": "Agency API key (kept only in Secret Manager; never logged)"
    },
    "agencyId": {
      "type": "string",
      "description": "Agency identifier (if required by GHL for scoping)",
      "pattern": "^[A-Za-z0-9_-]+$"
    },
    "scopes": {
      "type": "array",
      "description": "Requested capability scopes for token exchange",
      "items": { "type": "string" },
      "default": []
    },
    "tokenType": {
      "type": "string",
      "const": "agency_api_key",
      "description": "Credential type identifier for adapter discrimination"
    }
  },
  "required": [ "apiKey", "tokenType" ],
  "additionalProperties": false
}